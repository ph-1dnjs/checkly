# [개요] 시나리오 선택

> **범위**: 폴더 선택부터 하나 이상의 파싱된 시나리오를 실행 화면으로 전달할 때까지입니다.

폴더 경로는 `userData/scenario-folder.json`에 저장됩니다. 파일 목록은 `.md`, `.markdown` 확장자만 대상으로 수정 시각 내림차순으로 표시합니다. 파일 내용은 렌더러에서 캐시하고 `parseMarkdown`으로 시나리오를 만듭니다.

