# GitHub Releases auto-update configuration (required only for publishing updates)
GH_OWNER=your-github-owner
GH_REPO=your-repository

# Set only in CI or your local shell when publishing; never commit this value.
# GH_TOKEN=
